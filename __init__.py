"""Repo-local RouteDeck framework namespace.

This folder is intentionally separate from the SaaStoAgent app code so the
core contracts can later be packaged for PyPI/npm without dragging the product
runtime with them.
"""
