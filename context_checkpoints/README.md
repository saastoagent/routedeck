# Context Checkpoints

End-of-session restart checkpoints live here.

Use `context_checkpoint_DD-MM-YYYY-H-MMPM.md` filenames. Each checkpoint should
capture current state, completed work, in-progress work, blockers, validation,
and the first files to read next session.
