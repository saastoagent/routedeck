# RouteDeck Code Map

Last updated: 2026-07-15

This is the canonical subsystem-to-code map. Runtime and conversation
ownership follow
[ADR-006](../decisions/ADR-006-framework-owned-runtime-and-conversation-boundary.md).
[ADR-005](../decisions/ADR-005-operation-centric-state-and-consumer-structure.md)
retains its non-superseded structural decisions, and
[ADR-004](../decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md)
retains scope and local-execution authority. The executable clean break is
specified by the
[runtime-boundary refactor plan](../docs/superpowers/plans/2026-07-15-routedeck-runtime-boundary-refactor.md).
Earlier designs and completed plans remain historical implementation records.
Runtime and release verification are local Windows work; no remote runtime is
part of this architecture.

## How To Use This Map

- Start here before moving an interface across a package boundary.
- Keep the owning row's source globs, public interfaces, docs, and tests aligned.
- Run `python scripts/check_doc_coverage.py` as an advisory closeout check.
- Do not infer a release result from this map. Release evidence is produced by
  the release verifier for a specific run.

The `Source globs` column is parsed by `scripts/check_doc_coverage.py`; globs are
comma-separated and relative to this project root.

| Subsystem | Purpose | Source globs | Interfaces | Architecture anchors | Test anchors | Update triggers |
| --- | --- | --- | --- | --- | --- | --- |
| Compiled application, interaction kernel, and runtime | Product-neutral application compilation; canonical sessions; one supervised runner; navigation over that exact runner; configured projection; immutable services/runtime containers and explicit lifecycle. Compiler and supervision facades delegate to focused internal modules without alternate public implementations. | `routedeck_core/*.py`, `routedeck_core/**/*.py` | `compile_app`, `bind_app`, `CompiledRouteDeckApp`, `RouteDeckSessionAggregate`, `RouteDeckOperationRunner`, `RouteDeckRuntimeServices`, `RouteDeckRuntime`, `build_routedeck_runtime`, session/driver/store/notifier/clock/codec ports. | `architecture/components/core-runtime-contract.md`, `docs/route-deck-reference.md`, `decisions/ADR-005-operation-centric-state-and-consumer-structure.md`, `decisions/ADR-006-framework-owned-runtime-and-conversation-boundary.md` | `tests/state/test_runtime_builder.py`, `tests/app/**/*.py`, `tests/state/**/*.py`, `tests/supervision/**/*.py`, `tests/projection/**/*.py`, `tests/navigation/**/*.py` | Runtime container/builder, compiler facade, canonical contract, runner/navigation sharing, lifecycle, projection, review, effect, route, history, recovery, or adapter port changes. |
| Durable SQLAlchemy authority | Fail-closed runtime resource opening plus fenced SQLite/PostgreSQL persistence for sessions, turns, attempts, reviews, events, leases, private blobs, retention, migrations, and encryption. `store.py` is the canonical facade over explicit transaction services in `store_parts/`. | `routedeck_sqlalchemy/*.py`, `routedeck_sqlalchemy/**/*.py` | `open_sqlalchemy_routedeck_runtime`, `SqlAlchemyRuntimeResources`, `SqlAlchemySessionStore`, `FernetSensitiveCodec`, repository/runtime services, restart recovery, and instance-lease/database errors. | `architecture/components/core-runtime-contract.md`, `docs/route-deck-reference.md`, `decisions/ADR-005-operation-centric-state-and-consumer-structure.md`, `decisions/ADR-006-framework-owned-runtime-and-conversation-boundary.md` | `tests/sqlalchemy/**/*.py`, `tests/sqlite/test_persistent_runtime_smoke.py` | Resource opening/cleanup, facade delegation, transaction, fencing, replay, reopen, outbox, encryption, retention, dialect, maintenance, or recovery changes. |
| Generic FastAPI and SSE transport | One runtime-derived `/api/routedeck` router for contract, sessions, operations/reviews, typed user/assistant conversation, events, private forms, and inspection. `router.py` owns the prefix and focused `routes/` modules own endpoint groups. | `routedeck_fastapi/*.py`, `routedeck_fastapi/**/*.py` | `RuntimeProvider`, `create_routedeck_router_from_runtime_provider`, `AssistantTurnRequest`, `ConversationTurnRequest`, `conversation_fingerprint`, `stream_agent_turn`, typed request models, response/error mapping, and SSE encoders/stream. | `architecture/components/core-runtime-contract.md`, `docs/route-deck-reference.md`, `decisions/ADR-006-framework-owned-runtime-and-conversation-boundary.md` | `tests/fastapi/**/*.py`, especially `tests/fastapi/test_conversation_turns.py`, plus `tests/state/test_public_exports.py` | Runtime derivation, endpoint, request fingerprint/replay/collision, cancellation/interruption, cookie, status mapping, SSE reset, form authorization, or projection envelope changes. |
| Optional LangGraph integration | Builds one generic driver from product-supplied user-message and assistant-initiation graphs, translates events into typed RouteDeck conversation events, reconstructs durable turns, injects default-deny context, and routes tools through the runtime's runner. It never owns product topology/prompts/models. | `routedeck_langgraph/*.py`, `routedeck_langgraph/**/*.py`, `skills/routedeck-langgraph-integration/SKILL.md` | `RouteDeckLangGraphGraphs`, `RouteDeckLangGraphDriverFactory`, `RouteDeckLangGraphAgentDriver`, `RouteDeckMiddleware`, `RouteDeckToolWrapper`, `RouteDeckInvocationContext`, `awrap_tool_call`, `build_model_context`, conversation extraction. | `architecture/components/langgraph-adapter.md`, `skills/routedeck-langgraph-integration/SKILL.md`, `docs/route-deck-reference.md`, `decisions/ADR-006-framework-owned-runtime-and-conversation-boundary.md` | `tests/test_langgraph_agent_driver.py`, `tests/test_langgraph_model_context.py`, `tests/test_langgraph_policy_prompt.py`, `examples/medusa-agent/backend/tests/contract/test_agent_middleware.py` | Trigger/graph factory, event translation/cleanup, assistant no-tool contract, context allowlist, conversation reconstruction, or tool supervision changes. |
| Headless TypeScript runtime | Strict decoder modules behind one barrel; typed HTTP/SSE, chat, assistant-turn, and history clients; authoritative observable state; exact routing/history; forms; retained requests; and focused bootstrap/synchronization/operation/lifecycle coordinators behind `createRouteDeckStore`. | `packages/core/**/*.ts`, `packages/core/**/*.json` | `@routedeck/core`, `createRouteDeckClient`, `createRouteDeckAgentClient`, `streamAssistantTurn`, `createRouteDeckStore`, `RouteDeckObservableState`, routing/event coordinators, private-form client/state. | `architecture/components/react-runtime-debugger.md`, `docs/route-deck-reference.md`, `docs/using-routedeck.md`, `decisions/ADR-005-operation-centric-state-and-consumer-structure.md`, `decisions/ADR-006-framework-owned-runtime-and-conversation-boundary.md` | `packages/core/src/**/*.test.ts`, `packages/testing/tests/**/*.ts`, Medusa frontend reliability/routing tests | Strict decode parity, assistant/chat streams, store coordinator, event ordering, resync, route/history, form, retained-request, or request-ID changes. |
| React primitives | Generic provider, named conversation presentation actions plus network lifecycle hook, surface registry/host, forms, review, navigation, status, and lazy inspector. | `packages/react/**/*.ts`, `packages/react/**/*.tsx` | `@routedeck/react`, `ConversationPresentationActions`, `useConversationPresentation`, `useRouteDeckConversation`, `RouteDeckProvider`, surface/form/review/navigation/inspector primitives. | `architecture/components/react-runtime-debugger.md`, `docs/route-deck-reference.md`, `docs/using-routedeck.md`, `decisions/ADR-006-framework-owned-runtime-and-conversation-boundary.md` | `packages/react/src/**/*.test.ts`, `packages/testing/tests/**/*.ts`, `examples/medusa-agent/frontend/src/tests/**/*.tsx` | Named presentation method, abort/retry/resync lifecycle, slot ordering, affordance, review, form, navigation, error, or inspector changes. |
| Shared testing support | Explicit conformance utilities and test-only scripted models/harnesses. No fixture or scripted model belongs in a product runtime path. | `routedeck_testing/*.py`, `routedeck_testing/**/*.py`, `packages/testing/**/*.ts`, `packages/testing/**/*.tsx` | Python conformance helpers; `@routedeck/testing` component, SSE, store, and factory utilities. | `docs/route-deck-reference.md`, `test_index/README.md`, `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md` | `tests/**/*.py`, `packages/testing/tests/**/*.ts` | Test-port, fixture isolation, conformance assertion, or public testing export changes. |
| Standalone Medusa buyer agent | Product-owned guest-buyer logic and UI over real local Medusa: declarations/bindings/callbacks, product graph/prompt/models, market facts, typed Store client/resources, readiness, commerce operations, and buyer rendering. It supplies inputs to RouteDeck factories but owns no generic runtime/driver/transport construction. | `examples/medusa-agent/**/*.py`, `examples/medusa-agent/**/*.ts`, `examples/medusa-agent/**/*.tsx`, `examples/medusa-agent/**/*.json`, `examples/medusa-agent/**/*.toml`, `examples/medusa-agent/**/*.yaml`, `examples/medusa-agent/**/*.yml`, `examples/medusa-agent/**/*.ps1`, `examples/medusa-agent/**/*.sh`, `examples/medusa-agent/**/*.md`, `examples/medusa-agent/**/Dockerfile`, `examples/medusa-agent/**/.dockerignore` | `MedusaStoreClient`, `HttpMedusaStoreClient`, resource facades, feature specs/operations, `MEDUSA_APP_SPEC`, `bind_medusa_app`, session/graph factories, `open_live_medusa_application`, product health/readiness, React buyer surfaces, protected stack. | `docs/medusa-agent-reference-app.md`, `examples/medusa-agent/README.md`, `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md`, `decisions/ADR-006-framework-owned-runtime-and-conversation-boundary.md` | `examples/medusa-agent/backend/tests/**/*.py`, `examples/medusa-agent/frontend/src/tests/**/*.tsx`, focused real-Medusa/browser gates | Store resource/wire schema, feature binding, session/graph/configuration, market, commerce operation, product health/readiness, surface, protected fixture, browser, or source-of-truth changes. |
| Packaging and public docs | Python/npm metadata, canonical exports, generated contracts, license/notices, and public release posture. | `pyproject.toml`, `package.json`, `pnpm-workspace.yaml`, `packages/*/package.json`, `packages/*/src/index.ts`, `README.md`, `LICENSE`, `THIRD_PARTY_NOTICES.md`, `docs/*.md` | Root Python `ApplicationSpec`/`FeatureSpec` compiler surface and optional extras; `@routedeck/core`, `@routedeck/react`, private `@routedeck/testing`; contract generation scripts. | `architecture/components/packaging-public-readiness.md`, `README.md`, `docs/packaging-roadmap.md` | `tests/test_public_api.py`, package-owned tests/typecheck/build/dry-pack checks, clean-install release gate | Package name/export, version, dependency, license, install, generated contract, quickstart, or public boundary changes. |
| Architecture and context authority | Code map, component contracts, ADR/design/plan chain, restart context, flow index, lifecycle folders, and validation index. | `architecture/**/*.md`, `critical_prompt.md`, `context.md`, `context_pipeline.md`, `instructions.md`, `work_prompt.md`, `structure.md`, `SYSTEM_FLOW_INDEX.md`, `test_index/**/*.md`, `decisions/**/*.md`, `docs/superpowers/**/*.md`, `logs/**/*.md`, `context_checkpoints/**/*.md`, `context_history/**/*.md`, `plans/**/*.md`, `knowledgebase/**/*.md`, `audits/**/*.md`, `errors/**/*.md` | ADR-006 runtime/conversation authority; ADR-005 non-superseded structure; ADR-004 scope/local execution; runtime-boundary plan; historical completed plans. | `README.md`, `architecture/code-map.md`, `architecture/components/README.md`, `context_pipeline.md` | `tests/test_active_design_authority.py`, `scripts/check_doc_coverage.py`, schema-3 `runtime_ownership` boundary report | Authority, ownership, local-runtime policy, validation command, source tree, plan, or handoff changes. |
| Validation and release tooling | Framework, boundary, coverage, protected-stack, browser, release-summary, and sanitized evidence contracts. | `tests/*.py`, `tests/**/*.py`, `scripts/*.py`, `vitest.config.ts`, `examples/medusa-agent/scripts/*.py`, `examples/medusa-agent/scripts/*.ps1` | Pytest, project-scoped Vitest/V8 coverage, coverage checker, protected stack actions, release verifier and summary bundle. | `test_index/README.md`, `docs/medusa-agent-reference-app.md`, `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md` | `tests/test_release_harness.py`, all framework and Medusa suites | Gate, threshold, command, environment proof, test discovery, stack cleanup, evidence schema, or sanitization changes. |

## Closeout Rule

For each changed source file, name the owning row and either update its docs and
test anchors or state why they remain correct. Generated output and release
evidence never replace source-level tests or a real-source-of-truth smoke.
