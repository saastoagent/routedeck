# RouteDeck Code Map

Last updated: 2026-07-10

This is the canonical subsystem-to-code map for RouteDeck. It is intentionally
subsystem-level, not per-file prose.

## How To Use This Map

- Start here when changing RouteDeck source, examples, tests, or packaging.
- Update the owning row when code ownership, public interfaces, tests, or docs
  move.
- During closeout, list changed source files and either update the related
  architecture/test anchors or explicitly state that the anchors are unchanged.
- Run `python scripts/check_doc_coverage.py` before closeout for an advisory
  drift report.

## Coverage Table

The `Source globs` column is parsed by `scripts/check_doc_coverage.py`. Keep
globs comma-separated and relative to this project root.

| Subsystem | Purpose | Source globs | Interfaces | Architecture anchors | Test anchors | Update triggers |
| --- | --- | --- | --- | --- | --- | --- |
| Core contracts, compiler, runtime, and event kernel | Product-neutral application specification, client-contract export, runtime state, projections, operation/guard/review metadata, surfaces, typed events, channel filtering, and validation helpers. Current source contains the contract/runtime foundation; the server-authoritative kernel and complete event model are active refactor targets. | `routedeck_core/*.py`, `routedeck_core/**/*.py` | `RouteDeckApp`, `RouteDeckAppSpec`, `RouteDeckClientContract`, `RouteDeckInteractionRuntime`, operation/surface/event models, executor/backend protocols, validation helpers. | `architecture/components/core-runtime-contract.md`, `docs/agentic-ui-state-runtime.md`, `docs/framework-architecture.md`, `docs/boundary.md`, `decisions/ADR-002-two-adoption-modes-one-kernel.md` | `tests/test_core_contract.py`, `tests/test_projection_contract.py`, `tests/test_runtime_store_contract.py`, `tests/events/**/*.py`, planned kernel/client-contract suites | Application specification, client export, model shape, validation semantics, operation readiness, projection, surface, event envelope/channel/replay, or runtime state changes. |
| LangGraph compiler and adapter | First-class LangGraph execution path for Full Flow plus adapter support for existing custom LangGraph graphs. Current source provides validation and common graph wiring; compilation/executor integration is the active refactor target. | `routedeck_langgraph/*.py` | `validate_langgraph_contract(...)`, `assert_route_transition(...)`, `build_route_deck_state_graph(...)`, planned Full Flow compiler and executor adapter. | `architecture/components/langgraph-adapter.md`, `docs/framework-architecture.md`, `docs/using-routedeck.md`, `decisions/ADR-001-langgraph-native-routedeck.md`, `decisions/ADR-002-two-adoption-modes-one-kernel.md` | `tests/test_langgraph_adapter.py`, planned Full Flow/custom-graph conformance suites | LangGraph dependency use, application compilation, custom-graph attachment, adapter validation, transition checks, graph builder behavior, or condition resolver policy changes. |
| Durable backend and FastAPI/SSE transport | Coordinated persistence and transport for robust Full Flow/Core Integration use: atomic session/result/event outbox, replay, product-neutral HTTP, and SSE. | `routedeck_sqlite/*.py`, `routedeck_sqlite/**/*.py`, `routedeck_fastapi/*.py`, `routedeck_fastapi/**/*.py` | `RouteDeckSqliteBackend`, `create_routedeck_router(...)`, client-contract/session/dispatch/review/inspect routes, typed event channel streams. | `architecture/components/core-runtime-contract.md`, `docs/framework-architecture.md`, `docs/route-deck-reference.md`, `decisions/ADR-002-two-adoption-modes-one-kernel.md` | `tests/sqlite/**/*.py`, `tests/events/**/*.py`, `tests/test_fastapi_transport.py` | Persistence schema, transaction/outbox boundary, replay/durability, HTTP contract, SSE encoding, auth dependencies, or channel mounting changes. |
| React runtime, store, and debugger | React package for RouteDeck store, event client/reducer, client-contract loader, surface registry, provider, hooks, location state, and debugger topology. | `react/src/*.ts`, `react/src/*.tsx`, `react/src/**/*.ts`, `react/src/**/*.tsx` | `@routedeck/react`, `RouteDeckProvider`, event/store/hooks, client-contract loader, surface registry, debugger components, location helpers, TypeScript types. | `architecture/components/react-runtime-debugger.md`, `docs/agentic-ui-state-runtime.md`, `docs/using-routedeck.md` | `react/tests/**/*.tsx`, `react/tests/**/*.mjs`, `react/package.json` | Event/store contract, client-contract parity, hook behavior, debugger routing/topology, operation readiness, location state, type exports, or package scripts. |
| Standalone adoption examples | Self-contained examples independent of Corpus that prove both developer journeys: Full Flow and Core Integration. | `examples/full-flow-change-planner/**/*`, `examples/core-integration-document-review/**/*` | Full Flow change-planner and existing-agent Core Integration document-review applications, each with backend, exported client contract, SSE/React UI, tests, containers, and smoke docs. | `architecture/components/examples-and-adoption.md`, `docs/minimal-example.md`, `docs/using-routedeck.md`, `decisions/ADR-002-two-adoption-modes-one-kernel.md` | Per-example backend/frontend tests, `tests/conformance/**/*.py`, clean-install and browser smoke | Example route shape, self-contained real behavior, adoption mode, event/SSE behavior, compose/Docker/env/package files, dependency lockfiles, clean-install smoke, or Corpus/product leakage changes. |
| Medusa reference example | Product-owned adoption proof for chat-first agent UI, read-only Medusa Store API catalog/media projection, planning context, literal navgraph orientation, debug context, and chat-to-projection convergence without moving Medusa behavior into RouteDeck framework code. | `examples/medusa-agent/**/*.py`, `examples/medusa-agent/**/*.ts`, `examples/medusa-agent/**/*.tsx`, `examples/medusa-agent/**/*.md`, `examples/medusa-agent/**/package.json`, `examples/medusa-agent/**/package-lock.json` | `/api/medusa-agent/agent/stream`, `/api/medusa-agent/projection`, `/api/medusa-agent/route-stream`, `/api/medusa-agent/debug/context-thread`, `open_medusa_surface`, Medusa planning context, browser path plus `surface_id` codec. | `docs/medusa-agent-reference-app.md`, `docs/superpowers/plans/2026-06-08-routedeck-medusa-micro-slices.md`, `critical_prompt.md`, `context.md`, `examples/medusa-agent/frontend/design-qa.md` | `examples/medusa-agent/backend/tests/*.py`, `examples/medusa-agent/frontend/src/App.test.tsx`, `tests/test_anti_drift_boundaries.py`, `tests/test_medusa_reference_slice0.py` | Medusa chat behavior, Store API read boundary, projection/surface rendering, action chips, product fact grounding, dependency lockfiles, chat-to-projection convergence, route-stream separation, debug context, product-owned API routes, or visible-slice readiness criteria. |
| Packaging and public readiness | Python/npm metadata, release posture, public docs, notices, and package export boundaries. | `pyproject.toml`, `react/package.json`, `README.md`, `LICENSE`, `THIRD_PARTY_NOTICES.md`, `docs/packaging-roadmap.md`, `docs/*.md` | Python package metadata, npm package metadata, README/docs public surface, license, and third-party notices. | `architecture/components/packaging-public-readiness.md`, `docs/packaging-roadmap.md`, `README.md` | `python -m pytest tests -q`, `cd react && npm test` | License/metadata, third-party notices, public docs, package name/export, private flag, install instructions, CI/release policy, or public scrub changes. |
| Repo-local skills and scaffolding | Repeatable RouteDeck workflows for manifest authoring, manifest scaffolding, and LangGraph integration. | `skills/**/*.md`, `skills/**/*.py`, `skills/**/*.json` | Repo-local skill `SKILL.md` files and scaffolder scripts. | `architecture/components/skills-and-context-architecture.md`, `README.md` | `python skills/routedeck-manifest-scaffolder/scripts/scaffold_manifest.py skills/routedeck-manifest-scaffolder/examples/basic-flow.json generated_manifest.py --force`, skill self-review | Skill trigger rules, stable inputs/outputs/checks, scaffolder behavior, or reusable workflow policy changes. |
| Architecture coverage docs | RouteDeck's code-referenced architecture map and component contracts. | `architecture/**/*.md` | Architecture index, code map, component docs, closeout ownership rules. | `architecture/README.md`, `architecture/code-map.md`, `architecture/components/README.md` | `python scripts/check_doc_coverage.py`, `python scripts/check_doc_coverage.py --files routedeck_core/models.py react/src/RouteDeckStore.ts` | Code-map row changes, component doc inventory changes, source ownership policy changes, or checker expectations. |
| Context architecture and handoff | RouteDeck-local restart context, session handoff, lifecycle docs, implementation plans, and context architecture workflow. | `critical_prompt.md`, `context.md`, `context_pipeline.md`, `instructions.md`, `work_prompt.md`, `structure.md`, `SYSTEM_FLOW_INDEX.md`, `test_index/**/*.md`, `logs/**/*.md`, `context_checkpoints/**/*.md`, `context_history/**/*.md`, `plans/**/*.md`, `docs/superpowers/plans/**/*.md`, `decisions/**/*.md`, `knowledgebase/**/*.md`, `audits/**/*.md`, `errors/**/*.md` | Context restart files, session prompts, lifecycle folder READMEs, validation index, flow index, and executable implementation plans. | `architecture/components/skills-and-context-architecture.md`, `context_pipeline.md`, `work_prompt.md` | `python scripts/check_doc_coverage.py`, `python scripts/check_doc_coverage.py --files critical_prompt.md` | Context file inventory, handoff workflow, lifecycle folder policy, validation index, flow index, plan, or closeout rules. |
| Tests and validation harness | Python and React contract/conformance tests that protect framework behavior. | `tests/*.py`, `tests/**/*.py`, `routedeck_testing/*.py`, `routedeck_testing/**/*.py`, `react/tests/*.tsx`, `react/tests/*.mjs`, `react/tests/**/*.tsx`, `react/tests/**/*.mjs`, `scripts/*.py` | Pytest, shared two-mode conformance harness, Node test runner, advisory doc coverage checker. | `architecture/code-map.md`, `architecture/components/README.md`, `test_index/README.md` | `python -m pytest tests -q`, `cd react && npm test` | Test movement, conformance behavior, validation command changes, added checker scripts, or acceptance criteria changes. |

## Closeout Rule

If a changed source file matches one of these rows, closeout must name the row
and say which architecture anchors and test anchors were updated or why they did
not need updates.
