# RouteDeck Code Map

Last updated: 2026-07-11

This is the canonical subsystem-to-code map for RouteDeck. It is intentionally
subsystem-level, not per-file prose.

Active authority follows
[ADR-004](../decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md)
to the approved
[RouteDeck and Medusa buyer-agent design](../docs/superpowers/specs/2026-07-11-routedeck-medusa-agent-design.md)
and its
[consumer-driven implementation plan](../docs/superpowers/plans/2026-07-11-routedeck-medusa-agent-implementation.md).
All planned runtime and release verification executes on the local Windows
development machine; the Mac mini is outside the active decision.

## How To Use This Map

- Start here when changing RouteDeck source, examples, tests, or packaging.
- Update the owning row when code ownership, public interfaces, tests, or docs
  move.
- During closeout, list changed source files and either update the related
  architecture/test anchors or explicitly state that the anchors are unchanged.
- Run `python scripts/check_doc_coverage.py` before closeout for an advisory
  drift report.

## Coverage Table

The `Source globs` column is parsed by `scripts/check_doc_coverage.py`. Keep
globs comma-separated and relative to this project root.

| Subsystem | Purpose | Source globs | Interfaces | Architecture anchors | Test anchors | Update triggers |
| --- | --- | --- | --- | --- | --- | --- |
| Core contracts, feature compiler, runtime, and event kernel | Product-neutral immutable feature specifications, runtime bindings, client-contract export, interaction/session state, projections, one supervised operation runner, review, surfaces, typed events, channel filtering, and validation helpers. Existing flat modules remain temporary Corpus compatibility facades while the approved package structure is built. | `routedeck_core/*.py`, `routedeck_core/**/*.py` | `RouteDeckAppSpec`, compiled application/features, runtime bindings, `RouteDeckOperationRunner`, operation/surface/event/session/failure models, executor/store/notifier protocols. | `architecture/components/core-runtime-contract.md`, `docs/agentic-ui-state-runtime.md`, `docs/framework-architecture.md`, `docs/boundary.md`, `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md` | `tests/app/**/*.py`, `tests/state/**/*.py`, `tests/navigation/**/*.py`, `tests/supervision/**/*.py`, `tests/projection/**/*.py`, `tests/events/**/*.py` | Feature specification, client export, model shape, validation, operation readiness, supervision, projection, surface, event envelope/channel/replay, or runtime state changes. |
| Optional LangGraph middleware | Runtime-neutral middleware that translates LangGraph model/tool activity into the same supervised operation runner used by UI actions. RouteDeck core does not import LangGraph, and the Medusa product owns its prompt, model, and graph. | `routedeck_langgraph/*.py`, `routedeck_langgraph/**/*.py` | LangGraph middleware, tool wrapper, and model-context adapter over compiled RouteDeck capabilities. | `architecture/components/langgraph-adapter.md`, `docs/framework-architecture.md`, `docs/using-routedeck.md`, `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md` | `tests/langgraph/**/*.py`, framework-boundary tests, Medusa agent contract tests | LangGraph dependency boundaries, tool-call supervision, model context, graph integration, or agent/UI operation-path parity changes. |
| Durable backend and FastAPI/SSE transport | Generic product-neutral persistence and transport: durable session/result/event outbox state, replay, instance fencing/CAS, HTTP contracts, private-form channel, and typed SSE. | `routedeck_sqlite/*.py`, `routedeck_sqlite/**/*.py`, `routedeck_fastapi/*.py`, `routedeck_fastapi/**/*.py` | SQLite session store and codec/lease interfaces; generic session/projection/dispatch/review/inspect/private-form routes and typed event streams. | `architecture/components/core-runtime-contract.md`, `docs/framework-architecture.md`, `docs/route-deck-reference.md`, `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md` | `tests/sqlite/**/*.py`, `tests/events/**/*.py`, `tests/fastapi/**/*.py` | Persistence schema, transaction/outbox boundary, replay/durability, instance lease, HTTP contract, SSE encoding, auth dependencies, private-form transport, or channel mounting changes. |
| Headless and React frontend runtime | Headless contracts/client/store/routing plus React provider, hooks, surfaces, navigation, private forms, review, status, and inspector. The old `react/` tree remains transitional until useful behavior is migrated and replacement tests pass. | `packages/core/**/*.ts`, `packages/core/**/*.tsx`, `packages/react/**/*.ts`, `packages/react/**/*.tsx`, `react/src/*.ts`, `react/src/*.tsx`, `react/src/**/*.ts`, `react/src/**/*.tsx` | `@routedeck/core`, `@routedeck/react`, generated client contracts, authoritative event store, surface registry, private-form channel, navigation, review, status, and inspector primitives. | `architecture/components/react-runtime-debugger.md`, `docs/agentic-ui-state-runtime.md`, `docs/using-routedeck.md`, `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md` | `packages/core/src/**/*.test.ts`, `packages/react/src/**/*.test.tsx`, transitional `react/tests/**/*.tsx`, Medusa frontend/browser suites | Client-contract parity, event/store ordering, hook behavior, surfaces/private forms/review, inspector topology, navigation/deep links, type exports, or package scripts. |
| Medusa reference example and standalone buyer agent | First consumer and portability proof for the complete guest-buyer flow against real local Medusa: discovery, cart, private checkout, reviewed placement, verified confirmation, agent/UI path parity, and chat-to-projection convergence without moving commerce behavior into RouteDeck. | `examples/medusa-agent/**/*.py`, `examples/medusa-agent/**/*.ts`, `examples/medusa-agent/**/*.tsx`, `examples/medusa-agent/**/*.md`, `examples/medusa-agent/**/package.json`, `examples/medusa-agent/**/package-lock.json`, `examples/medusa-agent/**/*.yaml`, `examples/medusa-agent/**/*.yml` | Product-owned Store client and feature handlers/providers/guards; injected executor; product chat API; generic RouteDeck session/projection/event/dispatch transport; Medusa product surfaces and browser routes. | `docs/medusa-agent-reference-app.md`, `docs/superpowers/specs/2026-07-11-routedeck-medusa-agent-design.md`, `docs/superpowers/plans/2026-07-11-routedeck-medusa-agent-implementation.md`, `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md`, `critical_prompt.md`, `context.md` | `examples/medusa-agent/backend/tests/**/*.py`, `examples/medusa-agent/frontend/src/tests/**/*.tsx`, `examples/medusa-agent/e2e/**/*.ts`, framework/Medusa boundary checks, real-Medusa integration and release gates | Store API source of truth, feature composition, product/runtime ownership, private data, supervised agent/UI operations, projection/surfaces, chat-to-projection convergence, SSE/replay, deep links, recovery, product-owned routes, containers, or release proof. |
| Packaging and public readiness | Python/npm metadata, release posture, public docs, notices, and package export boundaries. | `pyproject.toml`, `react/package.json`, `README.md`, `LICENSE`, `THIRD_PARTY_NOTICES.md`, `docs/packaging-roadmap.md`, `docs/*.md` | Python package metadata, npm package metadata, README/docs public surface, license, and third-party notices. | `architecture/components/packaging-public-readiness.md`, `docs/packaging-roadmap.md`, `README.md` | `python -m pytest tests -q`, `cd react && npm test` | License/metadata, third-party notices, public docs, package name/export, private flag, install instructions, CI/release policy, or public scrub changes. |
| Repo-local skills and scaffolding | Repeatable RouteDeck workflows for manifest authoring, manifest scaffolding, and LangGraph integration. | `skills/**/*.md`, `skills/**/*.py`, `skills/**/*.json` | Repo-local skill `SKILL.md` files and scaffolder scripts. | `architecture/components/skills-and-context-architecture.md`, `README.md` | `python skills/routedeck-manifest-scaffolder/scripts/scaffold_manifest.py skills/routedeck-manifest-scaffolder/examples/basic-flow.json generated_manifest.py --force`, skill self-review | Skill trigger rules, stable inputs/outputs/checks, scaffolder behavior, or reusable workflow policy changes. |
| Architecture coverage docs | RouteDeck's code-referenced architecture map and component contracts. | `architecture/**/*.md` | Architecture index, code map, component docs, closeout ownership rules. | `architecture/README.md`, `architecture/code-map.md`, `architecture/components/README.md` | `python scripts/check_doc_coverage.py`, `python scripts/check_doc_coverage.py --files routedeck_core/models.py react/src/RouteDeckStore.ts` | Code-map row changes, component doc inventory changes, source ownership policy changes, or checker expectations. |
| Context architecture and handoff | RouteDeck-local restart context, authority chain, session handoff, lifecycle docs, approved design, executable plan, and context architecture workflow. | `critical_prompt.md`, `context.md`, `context_pipeline.md`, `instructions.md`, `work_prompt.md`, `structure.md`, `SYSTEM_FLOW_INDEX.md`, `test_index/**/*.md`, `logs/**/*.md`, `context_checkpoints/**/*.md`, `context_history/**/*.md`, `plans/**/*.md`, `docs/superpowers/plans/**/*.md`, `docs/superpowers/specs/**/*.md`, `decisions/**/*.md`, `knowledgebase/**/*.md`, `audits/**/*.md`, `errors/**/*.md` | ADR-004 -> approved design -> active plan; context restart files, session prompts, lifecycle folder READMEs, validation index, flow index, and executable implementation plans. | `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md`, `docs/superpowers/specs/2026-07-11-routedeck-medusa-agent-design.md`, `docs/superpowers/plans/2026-07-11-routedeck-medusa-agent-implementation.md`, `architecture/components/skills-and-context-architecture.md`, `context_pipeline.md`, `work_prompt.md` | `python -m pytest tests/test_active_design_authority.py -q`, `python scripts/check_doc_coverage.py` | Context file inventory, authority chain, local runtime policy, handoff workflow, lifecycle folder policy, validation index, flow index, plan, or closeout rules. |
| Tests and validation harness | Python and frontend contract/conformance tests that protect framework behavior and the Medusa consumer boundary. | `tests/*.py`, `tests/**/*.py`, `routedeck_testing/*.py`, `routedeck_testing/**/*.py`, `packages/testing/**/*.ts`, `packages/testing/**/*.tsx`, `packages/**/*.test.ts`, `packages/**/*.test.tsx`, `react/tests/*.tsx`, `react/tests/*.mjs`, `react/tests/**/*.tsx`, `react/tests/**/*.mjs`, `scripts/*.py` | Pytest, framework/Medusa conformance and boundary checks, test-only scripted models/harnesses, frontend test runner, and advisory doc coverage checker. | `architecture/code-map.md`, `architecture/components/README.md`, `test_index/README.md`, `decisions/ADR-004-routedeck-medusa-consumer-driven-runtime.md` | `python -m pytest tests -q`, package frontend tests, Medusa backend/frontend/browser suites, release verification | Test movement, conformance or boundary behavior, validation command changes, test-only support, checker scripts, or acceptance criteria changes. |

## Closeout Rule

If a changed source file matches one of these rows, closeout must name the row
and say which architecture anchors and test anchors were updated or why they did
not need updates.
