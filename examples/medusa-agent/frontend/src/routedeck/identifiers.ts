export const MedusaOperationType = Object.freeze({
  CATALOG_LIST: "catalog.list",
  CART_OPEN: "cart.open",
} as const);

export const MedusaSurfaceType = Object.freeze({
  CHECKOUT_ORDER_REVIEW: "checkout.order_review",
} as const);
