# Context Before Medusa Recalibration

Archived before adding the 2026-06-09 anti-drift recalibration note to
`context.md`.

RouteDeck context already identified `examples/medusa-agent/` as the active
product reference example and described the corrected Slice 1 checkpoint as
chat-first, Foundation Agent-inspired, true app-owned SSE, static starter
prompt chips, read-only Route Map, read-only Inspector, URL-derived
`surface_id`, and no RouteDeck runtime/API, Store API, product writes,
checkout, admin, or fake deterministic command-router behavior.

The drift that triggered this archive: a product-neutral minimal FastAPI/React
RouteDeck demo was created and presented as the next visible slice even though
the requested visible slice was Medusa Agent powered by RouteDeck. The next
implementation must delete that invented demo, return to the Medusa Agent
example, and audit against `critical_prompt.md`, `docs/route-deck-reference.md`,
and the Medusa micro-slice plan before claiming readiness.
